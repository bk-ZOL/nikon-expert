# Nikon Expert — 迭代路线图

> 基于 V2.0（2026-04-26）后的规划，按可行性从高到低排序

---

## V2.1 引用一键跳原文

**优先级：P0 · 预计周期：1-2 周 · 可行性：高**

### 要解决的问题
工程师无法快速验证 AI 回答是否准确，只能看到文字引用但无法查看原文上下文。这是 RAG 系统落地最大的信任障碍。

### 方案
- 检索结果中的引用变为可点击链接
- 点击后在右侧面板内嵌 PDF.js 打开 PDF，自动定位到引用页码
- 左右分栏布局：左侧对话，右侧原文预览
- 支持 PDF 和 Markdown 两种来源

### 技术方案
```
┌──────────────────┬──────────────────────┐
│   对话 + 引用列表  │  PDF.js 原文预览     │
│   （Gradio 左栏）  │  （自动跳转到页码）   │
└──────────────────┴──────────────────────┘
```
- Gradio 自定义 HTML 组件嵌入 PDF.js viewer
- 引用元数据中已有 `page_start`，直接作为跳转锚点
- Markdown 来源则高亮对应段落

### 验收标准
- 点击引用，PDF 自动打开到对应页码
- 工程师可在 5 秒内确认 AI 回答是否有依据

---

## V2.2 故障排查多轮收敛对话

**优先级：P1 · 预计周期：2-3 周 · 可行性：中高**

### 要解决的问题
当前故障排查是单轮问答，无法根据初步排查结果进一步追问。实际排查是迭代收敛过程：现象 → 初判 → 操作 → 观察 → 再判断。

### 方案
- 故障排查模式升级为多步引导式对话
- 每轮根据当前信息推荐下一步操作（换件？调参？查手册？问同事？）
- 推荐结果带置信度和优先级排序
- 底部展示"已走过的排查路径"防止重复

### 前置工作
- 故障履历数据结构化：从自由文本提取为"现象→初判→操作→验证→根因"卡片格式
- 每条故障记录增加 `排查步骤` 数组字段
- 自动从历史排查记录中学习高频排查路径

### 技术方案
```
用户输入: "E-5301 报警"

系统响应:
├── 匹配到 3 条历史故障记录
├── 匹配到手册 Section 5.3（对焦系统校准）
│
├── 推荐操作 1 (置信度 0.85): 检查 Stage Z-interferometer 读数
├── 推荐操作 2 (置信度 0.72): 重新执行 AF calibration
├── 推荐操作 3 (置信度 0.60): 检查 WA 清洁度
│
└── 请选择操作或描述当前观察结果 → 进入下一轮
```

### 验收标准
- 模拟一个完整故障排查场景（3-5 轮对话），系统能逐步收敛到正确方向
- 推荐操作中至少 50% 与历史成功案例一致

---

## V3.0 多模态：电路图识别

**优先级：P2 · 预计周期：2-4 周（Level 1）· 可行性：中**

### 要解决的问题
手册中有大量电路图、光路图、装配图，纯文本检索无法覆盖这些视觉信息。

### 分层方案

| 层级 | 能力 | 周期 | 依赖 |
|------|------|------|------|
| Level 1 | Vision LLM 图文混合检索：上传图片 → 识别内容 → 关联文本 | 2-3 周 | 需接入多模态 LLM（本地 Qwen-VL 或云端 API） |
| Level 2 | 电路图结构化：自动识别元件、连线关系、标注 | 2-3 月 | 需要训练专用模型或用 LLM 辅助标注 |
| Level 3 | 图谱推理：基于电路拓扑做故障传播分析 | 暂不建议 | 研究阶段 |

### Level 1 技术方案
- 图片上传 → Vision LLM（Qwen2.5-VL 或 GPT-4V）生成图片描述
- 描述文本 Embedding 后存入向量库，与现有文本检索打通
- 查询时：用户上传故障现象照片 → 匹配相关电路图 → 返回图 + 文字解释

### 验收标准
- 上传一张电路图，系统能识别主要元件并关联到手册对应章节
- 查询"这个电路的作用"能返回图文混合回答

---

## 战略层备忘（不做功能，但需持续思考）

- **定位扩展**：从"Nikon 专用工具"进化为"半导体工程文档检索平台"，核心卖点是"强引用 + 可追溯"而非"绝对准确"
- **开源策略**：Open Core 模式 — RAG 框架和工具链开源，企业案例库私有。取决于个人品牌 vs 商业化方向
