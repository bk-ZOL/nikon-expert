# Nikon Expert — Python 依赖清单（venv）
# 安装：python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# 版本对齐 2026-07 实测可用环境（macOS Apple Silicon, Python 3.11）

# ── RAG 框架（LlamaIndex 0.14，按需装子包，不装 umbrella 伞包）──
# 注：不装 `llama-index` 元包——它强制 llms-openai<0.7，与外接大脑用的
#     openai-like(需 0.7.x) 冲突。代码只 import 具体子包，装子包即可。
llama-index-core==0.14.17
llama-index-llms-ollama==0.10.0
llama-index-embeddings-huggingface==0.7.0
llama-index-vector-stores-qdrant==0.10.0
llama-index-postprocessor-flag-embedding-reranker==0.5.0
llama-index-readers-file==0.5.6
llama-index-workflows==2.16.1

# ── 外接大脑（云端 provider）──────────────────────────────
llama-index-llms-anthropic==0.11.8
llama-index-llms-openai==0.7.9
llama-index-llms-openai-like==0.7.2
anthropic==0.116.0
openai==2.26.0

# ── 向量数据库 ───────────────────────────────────────────
qdrant-client==1.17.0

# ── 文档解析 ─────────────────────────────────────────────
pymupdf4llm==1.27.2.1
pdfplumber==0.11.9
openpyxl==3.1.5
python-docx==1.2.0

# ── Embedding / Reranker ─────────────────────────────────
FlagEmbedding==1.3.5
sentence-transformers==5.3.0
transformers==5.3.0
huggingface_hub>=0.23

# ── 深度学习（Apple Silicon 走 MPS）──────────────────────
torch==2.10.0

# ── API & UI ──────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
gradio==6.9.0
pydantic==2.12.5

# ── 渲染 & 工具 ──────────────────────────────────────────
Markdown==3.10.2
python-dotenv==1.2.2
pyyaml==6.0.3
loguru==0.7.3
tqdm==4.67.3
pandas==2.3.3
